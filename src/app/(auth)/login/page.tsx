import { LoginForm } from "@/components/auth/LoginForm";

/**
 * ログイン画面のroot component
 * LoginForm コンポーネントをラップして、props や state を管理する
 */
export default function LoginPage() {
    return (
        <LoginForm />
    )
}